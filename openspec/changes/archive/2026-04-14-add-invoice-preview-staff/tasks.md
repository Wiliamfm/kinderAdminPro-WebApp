## 1. Add Invoice Preview Handler

- [x] 1.1 Add a handler that opens a blank browser tab from the click event
- [x] 1.2 Resolve the invoice file URL with `getInvoiceFileUrl()`
- [x] 1.3 Redirect the opened tab to the PDF URL

## 2. Handle Errors In The Existing Invoice Modal

- [x] 2.1 Show an inline error when the browser blocks the new tab
- [x] 2.2 Close the blank tab if the file URL lookup fails

## 3. Update Invoice Row Action

- [x] 3.1 Keep the inline "Ver archivo" action in each invoice row
- [x] 3.2 Preserve the existing replace action next to the preview button
