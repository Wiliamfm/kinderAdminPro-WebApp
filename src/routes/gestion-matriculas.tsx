import ProtectedPage from "~/components/ProtectedPage";

export default function GestionMatriculasPage() {
  return (
    <ProtectedPage
      title="Gestion de matriculas"
      heading="Gestion de matriculas"
      description="Gestiona inscripciones y estados de matricula en un solo lugar."
    />
  );
}
